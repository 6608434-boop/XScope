# 📁 ПАПКА: config/

## 📌 НАЗНАЧЕНИЕ
Централизованное хранение всех настроек приложения. Разделено на секции для удобства поддержки и безопасности.

## 📋 СТРУКТУРА
    📁 config/
    │
    ├── 📄 __init__.py           # Сборка всех конфигов в единый объект
    ├── 📄 base.py               # Базовые настройки Flask
    ├── 📄 database.py           # Настройки подключения к БД
    ├── 📄 api_keys.py           # Ключи для внешних API
    ├── 📄 security.py           # Настройки безопасности
    ├── 📄 features.py           # Флаги функций (feature flags)
    │
    └── 📁 environments/         # Настройки для разных окружений
        ├── 📄 __init__.py
        ├── 📄 development.py    # Для разработки
        ├── 📄 production.py     # Для продакшна
        └── 📄 testing.py        # Для тестов

## 🎯 КАК ИСПОЛЬЗОВАТЬ
### Получить любой параметр:
from config import config
db_url = config.DATABASE_URL
secret_key = config.SECRET_KEY
if config.IS_DEVELOPMENT:
    print("Мы в разработке")

### Проверить флаги функций:
from config.features import FeatureConfig
if FeatureConfig.ENABLE_WILL:
    print("Уилл Купер доступен")
enabled_profiles = FeatureConfig.get_enabled_profiles()

### Создать клиент DeepSeek:
from config.api_keys import APIConfig
if APIConfig.check_deepseek():
    client_config = APIConfig.get_deepseek_config()

## 🔧 КАК ДОБАВИТЬ НОВУЮ НАСТРОЙКУ
1. Если настройка общая - добавь в base.py
2. Если связана с БД - добавь в database.py
3. Если это API ключ - добавь в api_keys.py
4. Если связано с безопасностью - добавь в security.py
5. Если это флаг функции - добавь в features.py
6. Если зависит от окружения - добавь в файлы в environments/

## 🔐 БЕЗОПАСНОСТЬ
- Все ключи загружаются из .env файла
- .env НЕ хранится в Git (в .gitignore)
- Пример переменных в .env.example
- В продакшне все ключи ОБЯЗАТЕЛЬНЫ

## 🌍 ОКРУЖЕНИЯ
| Окружение | Файл | Когда используется |
| Development | development.py | Локальная разработка |
| Production | production.py | На сервере Railway |
| Testing | testing.py | При запуске тестов |

Переключение через переменную FLASK_ENV:
export FLASK_ENV=development  # или production, testing

## ✅ ВАЛИДАЦИЯ
При импорте config автоматически проверяет:
- Наличие DATABASE_URL
- Предупреждает о стандартном SECRET_KEY
- Проверяет наличие API ключей
- Считает включенные профили

## 📦 ЗАВИСИМОСТИ
- Не зависит от других папок проекта
- Используется всеми остальными модулями
- Требует python-dotenv для загрузки .env

## 👨‍💻 АВТОР
Oliver Vance - при изменении конфигурации уведомить команду!

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ
2026-02-14 - Oliver Vance - Создание документации